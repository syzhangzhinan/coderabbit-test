export enum UserRole {
  Admin = 'admin',
  User = 'user',
  Guest = 'guest',
}

export enum OrderStatus {
  Pending = 'pending',
  Paid = 'paid',
  Shipped = 'shipped',
  Completed = 'completed',
}

export enum ProductCategory {
  Books = 'books',
  Electronics = 'electronics',
  Clothing = 'clothing',
  Food = 'food',
}
